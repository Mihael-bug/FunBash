Required packages:
-cmatrix
sudo apt install cmatrix

-cava
sudo apt install cava

-hyfetch(it doesn't have to be this one you can use and other fetch just remember to change it in the script.)
sudo apt install hyfetch

This script doesn't have any drastic purpose except to look pretty and be fast when running it. I usually run it when i don't have anything as my wallpaper.
